server.port=8081

# ──────────────────────────────────────────────────────────
# TODO: Replace these with your actual Supabase project values
# Dashboard → your project → Settings → API
# ──────────────────────────────────────────────────────────
supabase.url=https://YOUR_PROJECT_ID.supabase.co
supabase.anon-key=YOUR_ANON_PUBLIC_KEY
# service_role key — gives unrestricted access to all rows (bypasses RLS)
# Dashboard → Settings → API → service_role (keep this secret, never put in Android app)
supabase.service-role-key=YOUR_SERVICE_ROLE_KEY

# ──────────────────────────────────────────────────────────
# Admin login credentials (change these!)
# ──────────────────────────────────────────────────────────
admin.username=admin
admin.password=civic2024

# Vaadin
vaadin.launch-browser=true
vaadin.allowed-packages=com.vaadin,org.vaadin,dev.hilla,com.civicconnect
spring.mustache.check-template-location=false
